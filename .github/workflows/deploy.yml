name: 🚀 Deploy to Bluehost

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repo
        uses: actions/checkout@v3

      - name: Upload to Bluehost via SSH
        uses: easingthemes/ssh-deploy@v2.1.5
        with:
          SSH_PRIVATE_KEY: ${{ secrets.SFTP_PRIVATE_KEY }}
          REMOTE_HOST: ftp.cvj.hbq.mybluehost.me
          REMOTE_USER: richard
          TARGET: /home1/cvjhbqmy/habitareinmobiliaria.com.py/richard
          SOURCE: ./registro-habitare

