name: 🚀 Deploy via SFTP

on:
  push:
    branches:
      - main

jobs:
  deploy:
    name: Upload via SSH
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Deploy to Bluehost
        uses: easingthemes/ssh-deploy@v2.1.5
        with:
          ssh-private-key: ${{ secrets.SFTP_PRIVATE_KEY }}
          remote-user: richard@habitareinmobiliaria.com.py
          server-ip: ftp.cvj.hbq.mybluehost.me
          remote-path: /public_html/ventas
          local-path: ./registro-habitare



